apiVersion: noobaa.io/v1alpha1
kind: BucketClass
metadata:
  name: noobaa-default-bucket-class
  namespace: openshift-storage
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  placementPolicy:
    tiers:
      - backingStores:
          - noobaa-pv-backingstore

