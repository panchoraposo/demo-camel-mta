apiVersion: operator.openshift.io/v1
kind: Console
metadata:
  name: cluster
spec:
  plugins:
    - logging-view-plugin
    # Present by default on this cluster; keep them so Argo stays Synced.
    - monitoring-plugin
    - networking-console-plugin

